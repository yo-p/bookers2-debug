<div class="row">
<% if flash[:notice] %>
	<%= flash[:notice]%>
<% end %>
	<div class="main col-md-offset-3 col-md-6">
		<h1>welcome to<span class="glyphicon glyphicon-book"></span>
			<strong>Bookers</strong>  !!</h1>
			<p>In <span class="glyphicon glyphicon-book"></span><strong>Bookers</strong>,</p>
			<p>you can share and exchange your opinions , inpressions , and emotions</p>
			<p>about varioius books!</p>

			<div class="btn-wrapper">
				<div class="row">
				<% if user_signed_in? %>
					<p><%=link_to "Log in", user_path(current_user), class: "btn btn-info col-xs-10 col-xs-offset-1" %></p>
				</div>
				<div class="row">
				<% else %>
					<p><%=link_to "Log in", new_user_session_path(current_user), class: "btn btn-success col-xs-10 col-xs-offset-1" %></p>
				<% end %>
				</div>
			</div>
			<div class="btn-wrapper">
				<div class="row">
				<% if user_signed_in? %>
					<p><%=link_to "Sign up", user_path(current_user), class: "btn btn-info col-xs-10 col-xs-offset-1" %></p>
				</div>
				<div class="row">
				<% else %>
					<p><%=link_to "Sign Up", new_user_registration_path, class: "btn btn-success col-xs-10 col-xs-offset-1" %></p>
				<% end %>
				</div>
			</div>

	</div>
</div>